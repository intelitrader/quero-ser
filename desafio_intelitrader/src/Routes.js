import { Routes, Route, BrowserRouter } from 'react-router-dom';

export default function AppRoutes(){
    <BrowserRouter>
        <Routes>
            <Route/>
        </Routes>
    </BrowserRouter>
}