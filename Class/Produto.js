export default class Produto {
    constructor(codigo, quantidade, quantidadeMinima){
        this.codigo = codigo;
        this.quantidade = quantidade;
        this.quantidadeMinima = quantidadeMinima;
    }
}